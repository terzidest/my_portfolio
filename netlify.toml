# Netlify build configuration. The dashboard's branch/build settings should
# point at `main` so this file is the single source of truth for deploy config.

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
